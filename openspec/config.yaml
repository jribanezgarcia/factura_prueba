schema: spec-driven

# Project context (optional)
# This is shown to AI when creating artifacts.
# Add your tech stack, conventions, style guides, domain knowledge, etc.
# Example:
#   context: |
#     Tech stack: TypeScript, React, Node.js
#     We use conventional commits
#     Domain: e-commerce platform

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours

# Per-operation guidance (optional)
# Add advisory guidance for how apply and archive work should be conducted.
# This is separate from artifact rules above.
operations:
  apply:
    guidance:
      - Anade o actualiza el change en la seccion "En curso" de ESTADO.md, con una linea de que cambia.
      - Si el change cambia alguna norma de AGENTS.md (estilo, arquitectura o comentarios), actualiza AGENTS.md en el mismo change.
  archive:
    guidance:
      - Pasa el change a "Hecho" en ESTADO.md, actualiza "Que toca ahora" y apunta en "Trampas conocidas" lo que haya salido mal.
      - Archivar es mover una carpeta. Usa git add -A para que el commit registre tambien el borrado del origen.
      - Antes de terminar, git status --short debe quedar vacio y git ls-tree no debe devolver la carpeta origen.
      - Tras archivar, haz commit automaticamente. Despues pregunta al usuario si hace push; no hagas push sin su confirmacion.
